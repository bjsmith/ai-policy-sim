# Configuration for AI Policy Simulation

# Application settings
app:
  version: "1.0.0"

# Server settings
server:
  port: 5000
  host: "127.0.0.1"
  debug: true

# Simulation defaults
simulation:
  default_years: 10
  default_samples: 1000
  max_samples: 5000
